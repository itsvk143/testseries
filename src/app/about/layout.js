export const metadata = {
    title: 'Poll Test Series | Quality Tests for Quality Students',
    description: 'Poll Test Series provides subtopic-wise, chapterwise, subjectwise, Poll, live and full tests for serious NEET, JEE Main, JEE Advance, CUET, and BITSAT aspirants.',
    keywords: ['Poll Test Series', 'NEET test series', 'JEE Mains full tests', 'JEE Advance test series', 'CUET mock tests', 'BITSAT tests', 'chapterwise tests', 'subjectwise tests', 'subtopic practice', 'live tests', 'poll tests']
};

export default function AboutLayout({ children }) {
    return <>{children}</>;
}
