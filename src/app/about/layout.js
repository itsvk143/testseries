export const metadata = {
    title: 'Poll Test Series | Quality Tests for Quality Students',
    description: 'Poll Test Series provides subtopic-wise, topic-wise, chapter-wise, Poll, live and mock tests for serious NEET, JEE Main and BITSAT aspirants.',
    keywords: ['Poll Test Series', 'NEET test series', 'JEE Mains mock tests', 'BITSAT tests', 'chapterwise tests', 'subtopic practice', 'live tests', 'poll tests']
};

export default function AboutLayout({ children }) {
    return <>{children}</>;
}
